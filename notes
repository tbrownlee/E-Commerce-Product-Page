Do the icons need an id for icon-minus and icon-plus. Instead of class

Only numbers for input. 

Using this in places?

Make a transition root variable

Make other variables

See where I can use the ? : : instead of if/ else 

Ok foreal get rid of the hover shit- even if you like it, it is a NOOB MISTAKE. Employers wouldn't like it. 

Get rid of p in button? Header button